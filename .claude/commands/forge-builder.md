Launch standalone forge-builder. Read `.claude/skills/forge-builder/SKILL.md` and follow its workflow.

The build task is provided as arguments:
- `/forge-builder authentication middleware with JWT` → build that feature
- `/forge-builder add caching to the API` → build that feature

If no arguments, check for `architect/` directory with existing plans, or ask the user what to build.

$ARGUMENTS
